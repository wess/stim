guardrails_instruction = "Never auto-execute destructive operations (file deletion, force push, production deployment). Always confirm with the user first."
escalation_instruction = "If a step fails, stop and present: which step failed, the error reason, compressed context of what succeeded, and options: retry / skip / abort. Let the user decide."
