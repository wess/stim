{
  "name": "stim",
  "module": "src/main.ts",
  "version": "1.0.0b1",
  "type": "module",
  "scripts": {
    "dev": "bun run src/main.ts",
    "build": "bun build --compile --outfile=dist/stim src/main.ts",
    "build:all": "./scripts/build-all.sh",
    "compile": "./dist/stim compile"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
